import React, {Component} from 'react';
import {View, Text, Image, StyleSheet} from 'react-native';

function CourseInfo({navigation}) {
  return (
    <View>
      <Text>CourseInfo</Text>
    </View>
  );
}

export default CourseInfo;

const styles = StyleSheet.create({});
