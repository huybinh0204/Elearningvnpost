import React, {Component} from 'react';
import {View, Text, Image, StyleSheet} from 'react-native';

function CourseNotify({navigation}) {
  return (
    <View>
      <Text>CourseNotify</Text>
    </View>
  );
}

export default CourseNotify;

const styles = StyleSheet.create({});
